# Capability OS — Python dependencies
# Core (required)
bcrypt>=4.0
PyJWT>=2.8
uvicorn>=0.29
pytest>=7.4
pytest-cov>=4.0

# Accelerated vector search (100x faster semantic memory)
sqlite-vec>=0.1

# Optional — browser automation (only needed for browser tools)
# playwright>=1.40

# Optional — process listing (falls back to tasklist/ps)
# psutil>=5.9

# Optional — speech-to-text
# openai-whisper>=20231117
