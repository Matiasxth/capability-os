# Capability OS v3.2 — Python dependencies
#
# Install: pip install -r requirements.txt

# ── Core (required) ──
bcrypt>=4.0                  # Password hashing for auth
PyJWT>=2.8                   # JWT token generation/validation
uvicorn>=0.29                # ASGI server (async HTTP)
redis>=5.0                   # Message queue for distributed workers
anthropic>=0.40              # Anthropic Claude SDK (recommended LLM)
sqlite-vec>=0.1.9              # Accelerated vector search (100x faster)

# ── Testing ──
pytest>=7.4
pytest-cov>=4.0

# ── Optional ──
# psycopg[binary]>=3.1       # PostgreSQL driver (falls back to SQLite)
# httpx>=0.27                # Connection-pooled HTTP for LLM calls (falls back to urllib)
# playwright>=1.40           # Browser automation (Playwright backend)
# psutil>=5.9                # Process listing (falls back to tasklist/ps)
# openai-whisper>=20231117   # Speech-to-text (local Whisper model)
# google-generativeai>=0.5   # Google Gemini SDK (alternative to raw HTTP)
