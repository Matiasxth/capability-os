{
  "id": "network_http_get",
  "name": "HTTP GET",
  "category": "network",
  "description": "Perform HTTP/HTTPS GET with timeout.",
  "inputs": {
    "url": {"type": "string", "required": true, "description": "Target URL."},
    "headers": {"type": "object", "required": false, "description": "Optional request headers."}
  },
  "outputs": {
    "body": {"type": "string"},
    "status_code": {"type": "integer"},
    "headers": {"type": "object"}
  },
  "constraints": {
    "timeout_ms": 30000,
    "allowlist": ["http", "https"],
    "workspace_only": false
  },
  "safety": {
    "level": "medium",
    "requires_confirmation": false
  },
  "lifecycle": {
    "version": "1.1.0",
    "status": "ready"
  }
}
