[pytest]
testpaths = tests/unit
markers =
    integration: tests requiring full server or external services

# Coverage enforcement for the core package (spec section 31: core >= 80%).
# Run the full coverage check with:
#   python -m pytest --cov=system.core --cov-fail-under=80 --cov-report=term-missing:skip-covered
